import { BitmapAsset } from "@overreact/engine";

import backgroundUrl from "./background.png";
import lightsUrl from "./lights.png";

export const BACKGROUND_IMAGE: BitmapAsset = {
  url: backgroundUrl,
  size: [96, 16],
};

export const LIGHTS_IMAGE: BitmapAsset = {
  url: lightsUrl,
  size: [75, 12],
};
